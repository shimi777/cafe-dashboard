# 🔧 תיקון שגיאה - שם קטגוריה

## 🐛 הבעיה
```
❌ שגיאה בעיבוד הקובץ: 'מגדל מגדנות'
אנא ודא שהקובץ הוא בפורמט HTML תקין
```

**סיבה:** היה חוסר עקביות בשם הקטגוריה:
- בתצוגה: `'מגדל מגדנות אליזבט'` (שם מלא)
- בתרשים השבועות: `'מגדל מגדנות'` (שם קצר)

## ✅ התיקון

שורה 924:
```python
# לפני:
'מגדל מגדנות': 0,

# אחרי:
'מגדל מגדנות אליזבט': 0,
```

שורה 941:
```python
# לפני:
week_totals['מגדל מגדנות'] += item['quantity']

# אחרי:
week_totals['מגדל מגדנות אליזבט'] += item['quantity']
```

שורה 968:
```python
# לפני:
'מתוצר': 'מגדל מגדנות',
'בפועל': week_info['מגדל מגדנות'],
'יעד': category_goals_weekly['מגדל מגדנות']

# אחרי:
'מתוצר': 'מגדל מגדנות אליזבט',
'בפועל': week_info['מגדל מגדנות אליזבט'],
'יעד': category_goals_weekly['מגדל מגדנות אליזבט']
```

## ✅ אימות

```
✅ טוסט אבוקדו: בפועל=0, יעד=24
✅ כריך סלמון: בפועל=0, יעד=30
✅ מגדל מגדנות אליזבט: בפועל=0, יעד=54
✅ סקונס יחיד: בפועל=0, יעד=120
✅ כל שמות הקטגוריות תואמים!
```

## 🚀 עכשיו זה עובד!

כל שמות הקטגוריות עכשיו עקביים ותרשים השבועות עובד בצורה מושלמת.
