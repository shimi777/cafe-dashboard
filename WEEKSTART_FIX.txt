✅ תיקון בעיית WeekStart - Timedelta עם Series

═════════════════════════════════════════════════════════════════

🔴 הבעיה:
   שגיאה: TypeError: Invalid type <class 'pandas.core.series.Series'>
   שורה: trans_df['WeekStart'] = trans_df['Date'] - pd.Timedelta(...)
   
   גורם: pd.Timedelta() לא יכול לקבל Series, רק scalar

✅ הפתרון:

קודם:
─────────────────────────────────────────
trans_df['WeekStart'] = trans_df['Date'] - pd.Timedelta(
    days=trans_df['Date'].dt.weekday
)

אחרי:
─────────────────────────────────────────
trans_df['WeekStart'] = trans_df['Date'] - trans_df['Date'].dt.weekday.apply(
    lambda x: pd.Timedelta(days=x)
)

📝 הסבר:
   - trans_df['Date'].dt.weekday מחזיר Series (0-6 לימים)
   - pd.Timedelta() צופה scalar (מספר בודד)
   - apply() חוקק את pd.Timedelta לכל אלמנט בנפרד
   - התוצאה: Series של Timedeltas שאפשר לחסר מ-Date Series

═════════════════════════════════════════════════════════════════

✅ בדיקה:
   - סינטקס: ✅ תקין
   - Logic: ✅ נכון
   - WeekStart: ✅ יחושב כמו שצריך

═════════════════════════════════════════════════════════════════

🚀 עכשיו הדשבורד צריך לעבוד!

